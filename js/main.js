

var CLOCK; //for interval refresh; 
var REFRESH_RATE = 60; //fps
var REFRESH_INTERVAL = Math.floor(1000 / 60);  //frequency of updates per seconds to hit the DESIRED refresh LIMIT


//INITIAL SET
document.addEventListener("DOMContentLoaded", function(event) {

});